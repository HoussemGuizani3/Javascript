// Online Javascript Editor for free
// Write, Edit and Run your Javascript code using JS Online Compiler


function ConvertString(t){
    const result = [];
    var j = 0;
    
    for (var i = 0; i < n; i++ ){
        var ch = t[i].toUpperCase();
            result[j] = ch;
            j = j+1;
        
    }
    return result;
}

    const t = [];
    var n = Number(prompt("please give me the size of the table : "));
    
    for (var i = 0; i < n; i++){
        var nb = prompt("Enter item number  n " + (i + 1) + " : ");
        t[i] = nb;
    }
    
   var t2 = ConvertString(t);
   
    console.log("the first table is: ",t);
    console.log("the second table is: ", t2);
